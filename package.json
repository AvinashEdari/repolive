{
  "name": "repolive",
  "private": true,
  "workspaces": ["apps/web"],
  "scripts": {
    "dev:web": "npm run dev --workspace @repolive/web",
    "build:web": "npm run build --workspace @repolive/web",
    "check": "npm run lint --workspace @repolive/web && npm run typecheck --workspace @repolive/web"
  },
  "engines": { "node": ">=24" }
}

