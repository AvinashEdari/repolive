import type { Metadata } from "next";
import Link from "next/link";
import { LegalPage } from "../../components/legal-page";
export const metadata: Metadata = { title: "Security", description: "RepoLive security boundaries and vulnerability reporting.", alternates: { canonical: "/legal/security" } };
export default function SecurityPage() { return <LegalPage title="Security" summary="How RepoLive handles hostile repository input and responsible vulnerability reports."><h2>Core boundary</h2><p>RepoLive does not clone or execute repository code, install repository dependencies, follow URLs found in repository files, or serialize raw hydrated evidence. Retrieval uses the fixed official GitHub API with strict file, byte, path, and timeout limits.</p><h2>Accounts and secrets</h2><p>Supabase verifies account credentials. RepoLive verifies access-token signatures and scopes private history by user subject. Server credentials are not sent to the browser.</p><h2>Report a vulnerability</h2><p>Do not publish exploit details or access another user&apos;s data. Contact the repository owner privately through GitHub with safe reproduction steps and impact. See the project <Link href="https://github.com/AvinashEdari/repolive/blob/main/SECURITY.md">security policy</Link>.</p></LegalPage>; }
