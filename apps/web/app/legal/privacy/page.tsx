import type { Metadata } from "next";
import { LegalPage } from "../../components/legal-page";
export const metadata: Metadata = { title: "Privacy", description: "What RepoLive stores, excludes, shares, and retains.", alternates: { canonical: "/legal/privacy" } };
export default function PrivacyPage() { return <LegalPage title="Privacy" summary="What RepoLive stores, what it deliberately excludes, and how public sharing works."><h2>Information RepoLive stores</h2><p>RepoLive stores derived reports for public repositories, public GitHub metadata, bounded file paths and sizes, anonymous usage counters, and random public report identifiers. Signed-in history stores the Supabase user subject and links to saved reports.</p><h2>Information RepoLive does not store</h2><p>RepoLive does not store passwords, access tokens, server credentials, complete source code, raw hydrated evidence, submitted machine profiles, or pasted error text.</p><h2>Sharing and retention</h2><p>Analysis share URLs are public to anyone who has the link. Removing an item from account history removes the private history link, not the cached public report. The operational policy makes expired usage counters, old webhook receipts, old revoked API keys, and unowned cached reports eligible for cleanup after 90 days. Scheduling and final commercial retention approval remain operational launch requirements.</p><h2>Analytics</h2><p>Optional product analytics is disabled unless a server-side provider is configured. It accepts only named lifecycle events and categorical allowlisted fields; repository identities, contents, file paths, pasted errors, emails, cookies, and credentials are excluded.</p></LegalPage>; }
